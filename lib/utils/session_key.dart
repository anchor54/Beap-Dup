enum SessionKey {isLoggedIn,user,token,name,mobileNum,dob,userId,image,userName,title,bio,activityId,recipientId,habitName,goalId,invitedId,
  goalCount,every,everyfre,skin,repeat,startDate,time, selectedWeek,editFreq,editEveryType,editEveryFre,selectedId,selectedUserId,selectedUserName}
